# root:honor mode 0640; reconciler is a worker/celery process and needs only DB truth + broker dispatch.
DATABASE_APP_URL=postgresql://honor_app:REPLACE_ME@db.PROJECT_REF.supabase.co:5432/postgres?sslmode=require
REDIS_PASSWORD=REPLACE_ME_MIN_32_CHARS
