import { redirect } from 'next/navigation'; import { getOwnerSession } from '@/lib/auth';
export default async function AppHome(){const s=await getOwnerSession();if(!s)redirect('/login');return <section className="grid hero"><div><p className="eyebrow">C01 Foundation</p><h1>HONOR</h1><p className="muted">Authenticated infrastructure shell. Campaign intelligence, rendering, and final Polli experience arrive in later checkpoints.</p></div><div className="card"><div className="status"><span className="dot"/><strong>Owner session active</strong></div><p className="muted">Browser traffic remains same-origin and business calls flow through the Next.js BFF.</p></div></section>}
